/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/stopwatch',
  assetPrefix: '/stopwatch/',
};

export default nextConfig;